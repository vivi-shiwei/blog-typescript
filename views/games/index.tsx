import React, { useState } from 'react';

const Games:React.FC = () => {
  const [ i, setIi ] = useState(null);
  const o = { 'l': 'l', 'k': 0 };

  console.log({ 'bol': { 'k': 'k' } });
  return (
    <div>games</div>
  );
};

export default Games;
